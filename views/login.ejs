<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login | Issue Tracker</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding: 0;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, #eaf6ff 0%, #ffffff 100%);
    }

    .login-container {
      background: white;
      width: 360px;
      padding: 40px 35px;
      border-radius: 18px;
      box-shadow: 0 6px 18px rgba(0,0,0,0.1);
      text-align: center;
    }

    .logo {
      width: 70px;
      margin-bottom: 15px;
    }

    h2 {
      margin-bottom: 25px;
      color: #0194f3;
      font-weight: 600;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 15px;
      text-align: left;
    }

    label {
      font-weight: 500;
      color: #555;
      margin-bottom: 3px;
    }

    input[type="text"], input[type="password"] {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 14px;
      width: 100%;
      transition: 0.2s;
    }

    input[type="text"]:focus, input[type="password"]:focus {
      border-color: #0194f3;
      outline: none;
      box-shadow: 0 0 0 2px rgba(1,148,243,0.2);
    }

    button {
      background-color: #0194f3;
      color: white;
      border: none;
      padding: 10px;
      border-radius: 8px;
      font-size: 15px;
      font-weight: 500;
      cursor: pointer;
      transition: 0.2s;
      width: 100%;
    }

    button:hover {
      background-color: #007ad1;
    }

    .footer-text {
      margin-top: 20px;
      font-size: 13px;
      color: #777;
    }

    .footer-text a {
      color: #0194f3;
      text-decoration: none;
    }

    .footer-text a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <img src="https://ik.imagekit.io/traveloka/traveloka_logo_blue.png" alt="Logo" class="logo">
    <h2>Sign In</h2>

    <% if (typeof error !== 'undefined') { %>
      <p style="color:red; font-size:13px;"><%= error %></p>
    <% } %>

    <form method="POST" action="/login">
      <div>
        <label>Username</label>
        <input type="text" name="username" required>
      </div>

      <div>
        <label>Password</label>
        <input type="password" name="password" required>
      </div>

      <button type="submit">Login</button>
    </form>

    <div class="footer-text">
      <p>Issue Tracker &copy; <%= new Date().getFullYear() %></p>
    </div>
  </div>
</body>
</html>
