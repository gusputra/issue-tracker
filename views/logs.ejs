<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Activity Logs</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Poppins', sans-serif; background: #f3f6fb; margin: 0; }
    header { background: linear-gradient(90deg, #0098ff, #00b8f5); color: white; padding: 18px 40px; font-weight: 600; font-size: 18px; box-shadow: 0 3px 10px rgba(0,0,0,0.1); }
    .container { max-width: 900px; margin: 40px auto; background: white; border-radius: 16px; padding: 30px; box-shadow: 0 6px 16px rgba(0,0,0,0.08); }
    h2 { color: #0098ff; margin-bottom: 20px; }
    table { width: 100%; border-collapse: collapse; }
    th, td { padding: 10px; border-bottom: 1px solid #eee; text-align: left; font-size: 14px; }
    th { background-color: #f2f7ff; color: #333; }
    tr:hover { background-color: #f8fbff; }
    .back { display: block; text-align: center; margin-top: 20px; color: #555; text-decoration: none; }
  </style>
</head>
<body>
  <header>Activity Logs</header>
  <div class="container">
    <h2>System Logs</h2>
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>User</th>
          <th>Action</th>
          <th>Timestamp</th>
        </tr>
      </thead>
      <tbody>
        <% if (!logs || logs.length === 0) { %>
          <tr><td colspan="4" style="text-align:center;color:#888;">No activity recorded.</td></tr>
        <% } else { %>
          <% logs.forEach(log => { %>
            <tr>
              <td><%= log.id %></td>
              <td><%= log.user %></td>
              <td><%= log.action %></td>
              <td><%= log.timestamp %></td>
            </tr>
          <% }) %>
        <% } %>
      </tbody>
    </table>
    <a href="/" class="back">← Back to Dashboard</a>
  </div>
</body>
</html>
